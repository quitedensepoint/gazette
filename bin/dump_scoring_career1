#!/bin/bash 
if [[ $# != 1 ]] ; then
	echo "usage $0: campaign#"
	exit 1
fi

CAMPAIGN=$1

/usr/local/mysql/bin/mysqldump --opt -v -u root -pgravytrain community \
scoring_career_adversaries \
| gzip > /usr/local/community/dumps/career1_scoring_$CAMPAIGN.sql.gz &
